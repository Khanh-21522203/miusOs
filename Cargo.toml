[package]
name = "miusos"
version = "0.1.0"
edition = "2024"

[lib]
test = false
bench = false
doctest = false

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
